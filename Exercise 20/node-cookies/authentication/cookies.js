// const clearCookie = (res, cookieName) => {
//     res.clearCookie(cookieName, { path: '/' });
//   };
  
//   const setCookie = (res, cookieName, value, options = {}) => {
//     res.cookie(cookieName, value, { maxAge: 900000, httpOnly: true, ...options });
//   };
  
//   const getCookie = (req, cookieName) => {
//     return req.cookies[cookieName] || null;
//   };
  
//   module.exports = { clearCookie, setCookie, getCookie };
  